# Git Useful Commands (CLI):-

1. git init
2. git add f1.md
3. git rm --cached f1.md
4. git restore --staged filename.extension

# Commit
1. git commit -m "commit message"
2. git commit --amend -m "New Commit Message"
3. git commit --amend --no-edit

# Rename a Branch:-
1. git branch -m master  = respectfully
2. git branch -M master  = forcefully

# .gitignore file

# To connect Local repository (GENAI folder💻) to remote repository (GENAI github folder ☁️)
1. git remote add origin <link of your repo>

# Upload our code to github
1. git push -u origin main